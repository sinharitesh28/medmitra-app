const CONFIG = {
    API_BASE_URL: window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1'
        ? 'http://localhost:3000' 
        : 'https://136-116-93-95.sslip.io',
    BOT_USERNAME: 'MedMitra_bot' // Shifted from API
};